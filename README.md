# JavaScript Pair Sum Finder

A simple JavaScript function to find a pair with a given sum in an array.

## Usage

1. Clone the repository:

   ```bash
   git clone <repository-url>

   cd <repository-directory>
